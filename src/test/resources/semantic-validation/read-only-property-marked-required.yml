swagger: '2.0'
info:
  title: Test API
  description: Test API
  version: "1.0.0"
host: none
schemes:
  - https
basePath: /v1
produces:
  - application/json
paths:
  /test:
    get:
      operationId: test
      summary: test service
      responses:
        200:
          description: ok
          schema:
            type: string

definitions:
  Test:
    type: object
    required:
      - id
    properties:
      id:
        type: string
        readOnly: true
